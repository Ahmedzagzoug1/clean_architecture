class SliderObject {
  String title;
  String subTitle;
  String image;

  SliderObject(this.title, this.subTitle, this.image);
}
class SliderViewObject {
  SliderObject sliderObject;
  int numberOfSlider;
  int currentSlider;
  SliderViewObject(this.sliderObject,this.numberOfSlider,this.currentSlider);
}
